---
description: Run smoke tests and report results
allowed-tools: Bash(./scripts/*:*), Bash(bash scripts/*:*), Read
---

Run all smoke test scripts in `scripts/` and report the results.

If any checks fail:
1. Identify the root cause
2. Suggest how to fix it
3. Offer to fix it if the solution is straightforward

$ARGUMENTS
